export const weekends = () => {
  return ["Saturday", "Sunday"];
};

export const types = () => {
  return ["OPTIONAL", "FIXED"];
};

export const HOLIDAY_TYPES = {
  OPTIONAL: "OPTIONAL",
  FIXED: "FIXED",
};
